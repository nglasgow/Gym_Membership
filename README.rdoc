Gym Membership is an app one can use to log in as a user and join training classes.

I use 3 different models which are User, Training and Enrollment. Enrollment is my joint model. I am tracking which training the users are enrolled in.
